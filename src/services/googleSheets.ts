/**
 * Google Sheets Service Barrel Export
 * // ponytail: modularized into src/services/googleSheets/ (auth, core, mutations, analytics, types)
 * Maintains 100% backward compatibility for all existing imports.
 */

export * from './googleSheets/index';
