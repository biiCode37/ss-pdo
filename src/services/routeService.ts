/**
 * Route & Supabase Service Barrel Export
 * // ponytail: modularized into src/services/routes/ (routes, users, audit, sync, types)
 * Maintains 100% backward compatibility for all existing imports across the application and tests.
 */

export * from './routes/index';
